# ReLang Docs `tmp/`

Ce dossier est organise en deux zones:

1. `v0.1/`:
   1. documents retenus pour la version minimale v0.1,
   2. coherence validee avec les decisions recentes (execution snapshot-first, await explicite, timer primitif, model failure causal, inline vs spawn).
2. `hors-v0.1/`:
   1. documents historiques, exploratoires ou v0.2+,
   2. non consideres normatifs pour le perimetre minimal v0.1.

